import Login from "../../components/users/Login/Login";
import {Box} from "@mui/material";

const LoginPage = () => {
    return (
        <Box>
            <Login />
        </Box>
    );
};

export default LoginPage;